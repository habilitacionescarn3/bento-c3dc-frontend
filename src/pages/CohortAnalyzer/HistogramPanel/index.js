/**
 * Histogram panel module.
 *
 * Layout: chart/ (dataset chart + empty state + axis helpers), popup/ (expanded modal),
 * strip/ (reorderable row + beside-Venn portal), survival/ (KM + risk + layout fragments),
 * styles/ (styled-components + MUI), utils/ (DnD layout, downloads, modal KM helpers),
 * hooks/, plus Histogram.js and histogramConstants.js at this level.
 */
export { default } from './Histogram';
export * from './histogramConstants';
